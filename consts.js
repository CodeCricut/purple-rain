const MAX_STARTING_HEIGHT = 200;

const MAX_X_SPEED = 1;

const MIN_Y_SPEED = 8;
const MAX_Y_SPEED = 10;

const MIN_WIDTH = 2;
const MAX_WIDTH = 4;

const MIN_HEIGHT = 5;
const MAX_HEIGHT = 10;

const MAX_DROPS_PER_LINE = 50;
